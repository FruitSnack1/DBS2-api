<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet", href="css/bootstrap/bootstrap.css")
    link(rel="icon" href="images/logo.png")
    title Přihlášení
  body
    .container-fluid
      .row.mt-5
        .col-4.offset-4
          .card
            .card-body
              h5.card-title Přihlášení
              form(action="/login", method='post')
                .form-group
                  label(for="") Přihlašovací jméno
                  input.form-control(type="text", name="username" required)
                .form-group
                  label(for="") Heslo
                  input.form-control(type="password", name="password" required)
                button.btn.btn-primary.mb-2(type='submit') Přihlásit se
                br
                span Nemáte účet ? 
                a(href='/register') Registrovat se
